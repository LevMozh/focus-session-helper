# Focus Session Helper — VS Code Extension

### Автор: Можегов Лев М3105  
### IDE: Visual Studio Code  
### Репозиторий: https://github.com/LevMozh/focus-session-helper

---

## Описание проекта

**Focus Session Helper** — это расширение для Visual Studio Code, помогающее организовывать короткие и продуктивные фокус-сессии.  
Плагин добавляет в VS Code удобный таймер с отображением в статус-баре и возможностью быстро управлять своим рабочим временем.

Расширение ориентировано на студентов, разработчиков и всех, кто использует VS Code как основную среду работы.

---

# Возможности плагина

### Запуск таймера на выбранное количество минут  
Пользователь вводит длительность, и таймер начинает обратный отсчёт.

### Отображение времени в статус-баре  
Оставшееся время всегда видно внизу редактора.

### Пауза и возобновление  
Команда *Pause / Resume* позволяет приостановить и продолжить сессию одним нажатием.

### Остановка сессии  
В любой момент можно завершить сессию вручную.

### Статистика за сегодняшний день  
Плагин сохраняет время в `globalState` и позволяет посмотреть, сколько минут пользователь продуктивно работал сегодня.

---

# Принцип работы плагина

Расширение создаёт менеджер сессий, который:

1. Запускает внутренний таймер (`setInterval`)  
2. Каждую секунду обновляет оставшееся время  
3. Отображает статус в строке состояния VS Code  
4. Сохраняет тайминг в локальное хранилище редактора  

При этом файлы проекта остаются нетронутыми, а взаимодействие происходит только через команды и статус-бар.

---

# Архитектурные решения

### **1. FocusSessionManager**  
Отвечает за:
- запуск таймера  
- остановку  
- паузу  
- вычисление оставшегося времени  
- обновление UI  

### **2. StatsManager**  
Хранит статистику по дням с помощью встроенного механизма `globalState`.

### **3. StatusBarItem**  
Используется для отображения текста:
- оставшееся время  
- состояние (идёт / пауза)  

### **Почему такой подход?**

- Минимальная нагрузка  
- UI использует только статус-бар  
- Легко расширять  
- Полностью автономно  

---

# 🛠 Установка и запуск

### 1. Клонировать репозиторий
```bash
git clone https://github.com/LevMozh/focus-session-helper.git
```
### 2. Установить зависимости
```bash
npm install
```
### 3. Запустить плагин
Открыть проект в VS Code и нажать:
`F5`
Откроется новое окно VS Code с запущенным расширением.

